# Period 54: Metal 4 Command Model, Pipeline Datasets, Tensor, And ML

Status: complete.

Goal: close the final 20 Period 45 routes through exact composition, one
portable executable query addition, or precise unsupported decisions. A Metal
4 class or protocol is not itself a reason to add a broad feature flag.

## Phase Plan

### Phase 1: Contract And API Allocation

- Reuse `binding.ResourceTable` for the admitted argument-table semantic.
- Reuse the portable `sync` barrier contract for explicit ordering.
- Add exact occlusion counting under `diagnostics` without growing the root or
  common owner allowlists.
- Record every remaining missing owner/result/lifetime contract before code.

See `phase1.md`.

### Phase 2: Exact Counting Visibility

- Add Boolean versus exact-counting query mode.
- Gate counting independently from ordinary Boolean visibility.
- Lower counting to Metal counting visibility and Vulkan precise occlusion.

See `phase2.md`.

### Phase 3: Exact Compositions

- Bind Metal 4 argument-table semantics to the existing resource-table
  compatibility layer.
- Bind Metal 4 explicit-barrier semantics to the existing portable hazard and
  barrier contract.
- Keep Metal 4 object identity and native handles outside those claims.

See `phase3.md`.

### Phase 4: Precise Closure Decisions

- Close Metal 4 allocators, reusable command buffers/feedback, flexible
  pipelines, compiler/archive/datasets, counter heaps, tensor/ML, advanced
  reflection, and function logs where exact contracts are absent.
- Close pass-boundary and multi-counter/statistics shapes independently from
  executable timestamp and exact-count visibility.

See `phase4.md`.

### Phase 5: Evidence And Inventory Closeout

- Extend the physical offscreen query regression with exact sample counts.
- Update API/native inventories, the source ledger, routing, roadmap,
  checklist, usage docs, and changelog.
- Run all API/backend/package and physical Metal gates.

See `phase5.md` and `closeout.md`.

## Compatibility

The slice is additive and targets `v0.2.0`. It adds one diagnostics enum, one
defaulted query descriptor field, one capability field, and one typed error.
No root declaration, `Device`, `WindowContext`, `HeadlessContext`, or runtime
handle method is added.
